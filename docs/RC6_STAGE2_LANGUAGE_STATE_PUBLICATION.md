# Stage 2 language-state joint publication

This bounded integration preserves callback control through pending abort and
checkpoint recovery, enforces numeric-input default qualifiers, infers supported
body-driven simple parameter constraints, and adds 44 independent mathematical
cases. The runtime owns transaction replay and native state. The host admits
completed generated execution boundaries and supplies typed dependency factories.

Exact host `81c5cfc37e5445dcc8ce806d286450b27848c080`, tree
`6eb3da1092a4feadd7759cdeda34b120ea658d80`, passed coordinated Linux run
[34171225525](https://github.com/s7cret/OpenPine/actions/runs/34171225525).
Python 3.11 and 3.13 each executed 8470 tests with zero failures, errors or skips.
All 7754 previous tests remain; additions are 317 runtime, 150 frontend producer,
and 249 host tests. The five pre-existing provider network deselections remain.
Native protected workers, sandbox, architecture, conformance and package builds
passed. The frontend passed 152 Vitest and 22 Node tests plus its build.

Independent review reconstructed both exact 1332-file source archives, verified
source pins and all 830 observations per Python against immutable expected data:
47 previous builtin cases, 75 scalar cases, and 44 mathematical cases, each on
five paths. All paired reports were byte-identical. The twelve frozen Stage 1
observations and original corpus also passed unchanged on both interpreters.

## Exact tree integrations

| Repository | Release integration | Tested source |
| --- | --- | --- |
| pine2ast | `f6ccca777c3947974b07a3592491148f4d7ec51f` | `5fe76f6ce6a4e49b04d9bf66b6263f201ecffe0e` |
| pinelib | `f980e79ef2e09ac36233fa35dc303a32a61f0d86` | `fa7d7ab7f7ffdb1afd29f6167e61a6bb3f719ca7` |
| OpenPine | `ea63766a7b5899602c71420b1fac6152381cab92` | `81c5cfc37e5445dcc8ce806d286450b27848c080` |

Each integration retains the prior release as first parent and the tested source
as second parent. Its entire tree equals the verified tree; references advanced
without force. The unchanged compiler and other dependencies retain their exact
tested pins. This documentation publication is separate from implementation,
new tests and integration commits.

Preserved failures include the missing instrument-context test setup in the first
mathematical run, independent pending-abort counterexamples corrected before the
frozen runtime source, and native Windows resource/fcntl limitations. Expected
tables and prior assertions were not replaced by observed output. Linux evidence
is the acceptance authority for platform-specific execution paths.

Scalar cleanup is independently complete: eight archive tags preserve the eight
deleted temporary branch histories across four repositories. Release branches,
the permanent verification branch and subsequent feature branches were preserved.
Cleanup of this language-state block is pending.

Stage 2 remains in progress. Callable-result qualifier, varip nominal-array,
rolling-statistics and library-return context changes are excluded from this
tested source. All four original Stage 2 criteria still require full acceptance.
Manual engineering evidence does not establish TradingView execution parity.
Performance was not measured in this stage.
