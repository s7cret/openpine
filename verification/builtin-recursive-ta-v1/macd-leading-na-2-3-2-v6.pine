//@version=6
indicator("Independent recursive TA")
float sample = bar_index == 0 or bar_index == 1 ? na : close
[line, signal, histogram] = ta.macd(sample,2,3,2)
plot(line)
plot(signal)
plot(histogram)
